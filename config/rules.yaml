record_id:
  allow_null: false

customer:
  allow_null: false

amount:
  allow_null: false
  min: 0
  max: 100000

date:
  allow_null: false
  allow_future: false

status:
  allow_null: false
  allowed_values:
    - pending
    - paid
    - cancelled